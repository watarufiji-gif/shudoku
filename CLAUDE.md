# CLAUDE.md — 週読（Shūdoku）プロジェクト設計書

このファイルは、週読プロジェクトの全体構造・前提・進め方を記録した「地図」です。
Claude Code はこのファイルを毎回参照し、ここに書かれた構造から逸脱しないこと。

## プロジェクト概要

週読（Shūdoku）は、読書習慣から離れた社会人向けの「週刊・書評ニュースレター」サービス。
毎週土曜9時に1冊を紹介し、週末の読書・購入行動に合わせて更新する。
運営はソロ。決定志向で、モデルに合わない選択肢は素早く除外して実装に進む方針。

- コンテンツの3本柱：ビジネス書 / 小説 / 人文・哲学（曜日でローテーション）
- 収益モデル：**法人スポンサー＋一括（法人）購読のみ**。個人課金・単号販売・投げ銭は除外済み。
- そのため**検証済みメール購読者リスト（購読者数＋開封率）の構築が最重要**。
  スポンサー交渉ではSNSフォロワー数より購読者指標が効く。

## 技術スタック（確定）

- 開発：GitHub（watarufiji-gif/shudoku）, VSCode, Claude Code
- 作業ディレクトリ：~/Desktop/図書
- ホスティング：Netlify（site: syudoku2 / 本番ドメイン syudoku.com）
- CMS：microCMS（service: shudoku）— ヘッドレス運用。独自管理画面は当面作らない。
- DB：Supabase（project ID: gpmdnzcrklolwczoxybb）
- メール配信：Resend（contact@syudoku.com / ドメイン認証済み）
- 将来：Stripeは収益化フェーズまで保留。Next.js本格化時にVercel移行を検討。

## 3つの場所（混同しないこと）

1. **店頭 = Netlify**：来訪者が見る公開サイト。完成品を表示するだけ。
2. **在庫棚 = microCMS**：記事データの保管庫。サイトはここから記事を取り出して表示する。
3. **バックヤード = 実行環境（GitHub Actions想定）**：エージェントやスクリプトが動く場所。

## フェーズ進行（順番厳守：A→B→C→D）

この順番自体が戦略。Bを先に作ることで、CとDで集めた読者の価値を数字で証明できる。

- フェーズA：メール購読の土台（登録〜配信〜解除）＋特定電子メール法対応
- フェーズB：開封率・クリック率ダッシュボード（/admin）＝スポンサー営業の武器
- フェーズC：SEOアーカイブ（過去記事を検索・AIに拾われる資産にする）＝無料集客
- フェーズD：法人向けLP（/business）＋メディアキット（/media-kit）＝収益化

※この「フェーズA〜D」は実装タスクの区分であり、後述の「本選定エージェント」とは別軸。

## 本選定エージェント（ヒューマン・イン・ザ・ループ型）

毎週の「紹介する本」を、人間（運営者）が必ず承認する設計。フル自動投稿はしない。
エージェント＝「定時に起動し、下準備して、承認箱に置いて帰る店員」。

### 鉄則：生成と公開を分離する
エージェントは Supabase に下書き（status: pending）を置くところで必ず止まる。
microCMS → Netlify へ流れるのは、運営者が「承認」した後だけ。
承認ゲートがそのまま「サイトへの反映スイッチ」になる。

### 基本フロー
1. スケジューラ（GitHub Actions）が公開数日前に起動
2. 選定エージェントが候補から1冊を選び、理由・フック・SNS切り口・ファネル役割を付けて提案
3. 曜日別テンプレートで書評本文＋SNS投稿文を生成
4. Supabase に status: pending で保存（←ここで停止）
5. 運営者がレビュー画面で確認・編集し、承認 or 却下
6. 承認されたものだけが microCMS に登録 → Netlify 再ビルド → 土曜9時に公開

### 選定＝マーケティング判断（採点ルブリック）
「良い本」ではなく「ファネルを前進させる本」を選ばせる。各候補を次の軸で採点し理由を出す：
- 話題性・タイムリー性（新刊/著者が話題/映像化/季節性）
- 検索需要（長期のサイト流入＝リスト構築に複利で効く）
- フック強度（スクロールを止める1行が書けるか）
- ターゲット適合（時間がない/キャリア不安/お金/教養コンプレックス、特に「読書から離れた引け目」）
- シェア・保存性（社会的通貨／購入より摩擦の低い「保存」）
- 独自angle可能性（他の本アカウントが言わないことを言えるか）

初期の重み付けは「検索需要 × タイムリー性」を厚めにし、リスト構築を優先する。

### 学習ループ
方針が未確定なうちは、意図的にフック・ジャンル・切り口を散らす。
公開後に「どの回が登録・開封・シェアを伸ばしたか」を計測し、
その実績（performance）をエージェントの判断材料に戻す。勝ちパターンはデータで発見する。

### Supabase テーブル設計（本選定エージェント用）
- book_candidates：候補プール（title, author, genre, theme_tags[], pain_points[], status, source）
- selection_runs：選定実行ログ（候補群, agent_reasoning, marketing_scores(jsonb), chosen_id, status: pending/approved/rejected）
- published_books：公開済み（book_id, publish_date, hook, sns_angles(jsonb)）
- performance：実績（book_id, open_rate, signups, shares, saves, clicks）← 学習ループの燃料

### 本のデータソース方針
初期は「候補プール方式」を採用。運営者が20〜30冊を book_candidates に投入し、
エージェントは「選定＋文章生成」に集中する。実行時に web検索でトレンド信号（新刊・話題・ランキング）を近似で補う。

## 実装の進め方（人間用メモ）

- 1フェーズ／1ブロック＝1セッションで進め、終わるたびに git commit する。
- 「C（地図）→ A（エージェントが動く土台）→ B（承認→反映の配管）」のように、
  1ブロックずつ動作確認しながら繋ぐ。一気に全部繋がない（壊れた箇所を特定するため）。
- 各実装の前に「これから変更するファイル・追加カラム・保存先の一覧」を先に提示させる。
- 想定外の改変が混ざっていないか、変更一覧で毎回確認する。
